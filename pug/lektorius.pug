extends ./default.pug

block promo


block contentas
 section
  -var sk = apie.length
  each page, i in apie
   .r
    .c5.text-right.p-r
     .counter.post.rellax(data-rellax-speed='-1')
      if (sk < 10)
        !='0'+ (i+1) + '/0' + sk
      else
        != (i+1) + '/' + sk
     h2 #{page.skilties_pavadinimas}
     .pkr #{page.sritis}
    .c5.p-l != page.aprasymas
      
