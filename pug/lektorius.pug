extends ./default.pug

block promo
 

block contentas
 section
  -var sk = collections.lektorius.length
  each page, i in collections.lektorius
   .r
    .c5.text-right.p-r
     .counter.post.rellax(data-rellax-speed='-1')
      if (sk < 10)
        !='0'+ (i+1) + '/0' + sk
      else
        != (i+1) + '/' + sk
     h2 #{page.title}
     .pkr #{page.sritis}
    .c5.p-l
     != page.contents